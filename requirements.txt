# MonoStudio 26 — runtime deps (Qt UI)
PySide6>=6.6.0
requests>=2.28.0
python-mpv>=1.0.5
numpy>=1.26.0
opencolorio>=2.3.0

# Frameless window (PySide6 branch); Windows needs pywin32 for native resize/drag
pywin32; sys_platform == "win32"
# Windows shell thumbnail in Inspector (Inbox/Project Guide file preview)
comtypes; sys_platform == "win32"
windows-toasts>=1.3.1; sys_platform == "win32"
git+https://github.com/zhiyiYo/PyQt-Frameless-Window.git@PySide6

# Build / installer (optional, for maintainers)
# pyinstaller>=6.0.0
